package(
    default_visibility = ["//:gfxstream_packages"],
)

cc_library(
    name = "gfxstream_common_utils",
    srcs = ["Strings.cpp"],
    hdrs = glob(["include/**/*.h"]),
    includes = ["include"],
    copts = ["-fno-exceptions"],
)
