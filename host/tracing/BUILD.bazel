package(
    default_visibility = ["//:gfxstream_packages"],
)

cc_library(
    name = "gfxstream_host_tracing",
    srcs = ["Tracing.cpp"] + glob(["include/**/*.h"]),
    copts = ["-fno-exceptions"],
    includes = ["include"],
)
